---
phase: 01-fa-gui-shu-ju-ru-ku-di-zuo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bid_tool_agents/backend/app/domain/knowledge_ingest/models.py
  - bid_tool_agents/backend/app/domain/knowledge_ingest/loader.py
  - bid_tool_agents/backend/app/domain/knowledge_ingest/chunker.py
  - bid_tool_agents/backend/tests/knowledge_ingest/test_loader.py
autonomous: true
requirements:
  - INGEST-01
  - INGEST-03
must_haves:
  truths:
    - "系统可读取不标记版 JSONL 并逐行完成结构化解析。"
    - "任何缺失必填字段或 JSON 语法错误都会阻断导入并输出可定位错误。"
    - "每条法规记录都带有可追溯字段 title/chapter/source_file/chunk_id/version/article_no。"
  artifacts:
    - path: "bid_tool_agents/backend/app/domain/knowledge_ingest/models.py"
      provides: "法规记录与导入结果的数据契约"
    - path: "bid_tool_agents/backend/app/domain/knowledge_ingest/loader.py"
      provides: "JSONL 预检与结构化加载"
    - path: "bid_tool_agents/backend/app/domain/knowledge_ingest/chunker.py"
      provides: "条级切分与超长条语义分段"
    - path: "bid_tool_agents/backend/tests/knowledge_ingest/test_loader.py"
      provides: "INGEST-01/03 自动化回归"
  key_links:
    - from: "loader.py"
      to: "models.py"
      via: "LawRecord 构造"
      pattern: "LawRecord\\("
    - from: "loader.py"
      to: "chunker.py"
      via: "对 content 调用分段函数"
      pattern: "split_article\\("
    - from: "test_loader.py"
      to: "loader.py"
      via: "坏数据 fail-fast 断言"
      pattern: "pytest\\.raises"
---

<objective>
交付法规入库流水线的“输入预检 + 结构化标准化”底座，确保后续写库阶段只消费合法且可追溯的数据记录。

Purpose: 先把数据入口做成“可验证、可失败、可复现”的确定性流程，避免脏数据进入向量库。  
Output: 可复用的数据模型、加载器、切分器及对应自动化测试。
</objective>

<execution_context>
@/home/EDY/.cursor/get-shit-done/workflows/execute-plan.md
@/home/EDY/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-fa-gui-shu-ju-ru-ku-di-zuo/01-CONTEXT.md
@.planning/phases/01-fa-gui-shu-ju-ru-ku-di-zuo/01-RESEARCH.md
@.planning/phases/01-fa-gui-shu-ju-ru-ku-di-zuo/01-PATTERNS.md
@bid_tool_agents/backend/app/api/v1/knowledge.py
@bid_tool_agents/backend/app/tools/document/parser.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义法规记录与导入结果契约（实现 D-01/D-03/D-04）</name>
  <read_first>
    - bid_tool_agents/backend/app/api/v1/knowledge.py
    - .planning/phases/01-fa-gui-shu-ju-ru-ku-di-zuo/01-CONTEXT.md
  </read_first>
  <files>
    bid_tool_agents/backend/app/domain/knowledge_ingest/models.py
  </files>
  <action>
    创建 `LawRecord`、`IngestError`、`IngestSummary`、`IngestResult` 模型：`LawRecord` 必须包含 `title`、`chapter`、`article_no`、`content`、`source_file`、`chunk_id`、`version`、`full_text`、`chunk_text`，并新增 `validate_required_fields(record, source_file, line_no)` 校验函数；`source_file` 要求传入并保留仓库相对路径（D-04），`version` 必须作为必填字段（D-03），`article_no` 必须作为一等字段（D-01）。
  </action>
  <verify>
    <automated>cd bid_tool_agents/backend && pytest tests/knowledge_ingest/test_loader.py -k "model_contract" -x</automated>
  </verify>
  <acceptance_criteria>
    - `models.py` 导出 `LawRecord` 与 `IngestResult`，可被 `loader.py` 直接导入。
    - 当 `title/chapter/content/version/source_file` 任一为空时，`validate_required_fields` 抛出包含文件路径与行号的异常字符串。
    - `LawRecord` 实例可序列化为包含 `article_no/chunk_id/version/source_file` 的字典结构。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 JSONL 预检加载与稳定 chunk_id 生成（实现 D-02/D-14/D-15）</name>
  <read_first>
    - bid_tool_agents/backend/app/domain/knowledge_ingest/models.py
    - bid_tool_agents/backend/app/api/v1/files.py
    - .planning/phases/01-fa-gui-shu-ju-ru-ku-di-zuo/01-CONTEXT.md
  </read_first>
  <files>
    bid_tool_agents/backend/app/domain/knowledge_ingest/loader.py
  </files>
  <action>
    在 `loader.py` 中实现 `load_jsonl_records(input_paths: list[str], version: str) -> list[LawRecord]`：逐文件逐行 JSON 解析，先做 schema 预检再建模；缺字段或 JSON 解析失败时立即抛错并终止（fail-fast，D-14/D-15）；按 `sha256(title + "|" + chapter + "|" + content)` 计算稳定 `chunk_id`（D-02）；从内容中提取 `article_no`（优先原字段，其次正则抽取“第X条”）；`source_file` 必须转换为相对仓库根路径字符串（D-04）。
  </action>
  <verify>
    <automated>cd bid_tool_agents/backend && pytest tests/knowledge_ingest/test_loader.py -k "load_jsonl or fail_fast or stable_chunk_id" -x</automated>
  </verify>
  <acceptance_criteria>
    - 相同输入文件重复调用两次时，同一条记录的 `chunk_id` 完全一致。
    - 当任意一行为非法 JSON 时，函数抛错且错误信息包含该文件相对路径与具体行号。
    - 返回的记录列表中每条都包含非空 `version`、`source_file`、`chunk_id`、`article_no`。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 落地条级切分与超长条语义分段规则并补测试（实现 D-05/D-06/D-08）</name>
  <read_first>
    - bid_tool_agents/backend/app/tools/document/parser.py
    - bid_tool_agents/backend/app/domain/knowledge_ingest/loader.py
    - .planning/phases/01-fa-gui-shu-ju-ru-ku-di-zuo/01-RESEARCH.md
  </read_first>
  <files>
    bid_tool_agents/backend/app/domain/knowledge_ingest/chunker.py
    bid_tool_agents/backend/tests/knowledge_ingest/test_loader.py
  </files>
  <action>
    实现 `split_article(full_text: str, max_chars: int, overlap_chars: int) -> list[str]`：普通条文保持单 chunk（D-05），超长条文按句号/分号/换行优先的语义边界切段并追加 overlap（D-06）；在 `LawRecord` 组装阶段同时保留 `full_text` 与 `chunk_text`（D-08）；为“短条不切分、长条切分、overlap 生效、无语义边界时退化切分”编写测试。
  </action>
  <verify>
    <automated>cd bid_tool_agents/backend && pytest tests/knowledge_ingest/test_loader.py -k "split_article or overlap or full_text_chunk_text" -x</automated>
  </verify>
  <acceptance_criteria>
    - 短文本输入时返回长度为 1 的切分结果。
    - 长文本输入时返回多个片段，且相邻片段存在设定长度的重叠内容。
    - 每条待入库记录 payload 来源同时可取到完整原文与当前片段文本字段。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dataset files -> ingest loader | 不可信文件内容进入解析流程 |
| parsed record -> chunking pipeline | 未校验内容进入切分与建模 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `loader.py` JSONL 读取 | mitigate | 对每行执行 JSON 解析与必填字段校验，失败立即中断并标注文件+行号 |
| T-01-02 | I | `models.py` 元数据 | mitigate | 固定白名单字段写入 `LawRecord`，禁止将未知键透传到 payload |
| T-01-03 | D | `loader.py` 异常处理 | mitigate | fail-fast 终止导入，防止部分坏数据继续消耗下游资源 |
</threat_model>

<verification>
- `cd bid_tool_agents/backend && pytest tests/knowledge_ingest/test_loader.py -x`
</verification>

<success_criteria>
- 可用自动化测试证明：不标记版 JSONL 能被结构化解析并输出 `LawRecord` 列表（INGEST-01）。
- 每条记录包含 `title/chapter/source_file/chunk_id/version/article_no`，且字段值可复现（INGEST-03）。
- 坏数据输入会在预检阶段被阻断并提供精确定位信息。
</success_criteria>

<output>
After completion, create `.planning/phases/01-fa-gui-shu-ju-ru-ku-di-zuo/01-01-SUMMARY.md`
</output>
