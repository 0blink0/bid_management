---
phase: 01-fa-gui-shu-ju-ru-ku-di-zuo
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01-PLAN.md
files_modified:
  - bid_tool_agents/backend/app/tools/database/qdrant.py
  - bid_tool_agents/backend/app/domain/knowledge_ingest/vectorizer.py
  - bid_tool_agents/backend/app/domain/knowledge_ingest/writer.py
  - bid_tool_agents/backend/app/domain/knowledge_ingest/audit.py
  - bid_tool_agents/backend/app/scripts/ingest_laws.py
  - bid_tool_agents/backend/tests/knowledge_ingest/test_writer.py
  - bid_tool_agents/backend/tests/knowledge_ingest/test_idempotent_rebuild.py
autonomous: true
requirements:
  - INGEST-02
  - INGEST-04
  - INGEST-03
must_haves:
  truths:
    - "导入任务可将法规向量写入 `laws_regulations` 集合。"
    - "重建操作必须显式 force 才可执行删除并全量重建。"
    - "重复重建后集合数据量与标识可控且不发生不可控重复。"
    - "导入过程输出结构化摘要与错误明细。"
  artifacts:
    - path: "bid_tool_agents/backend/app/domain/knowledge_ingest/writer.py"
      provides: "force 门禁 + 重建 + 批量 upsert"
    - path: "bid_tool_agents/backend/app/domain/knowledge_ingest/audit.py"
      provides: "重建审计记录"
    - path: "bid_tool_agents/backend/app/scripts/ingest_laws.py"
      provides: "Phase 1 脚本入口"
    - path: "bid_tool_agents/backend/tests/knowledge_ingest/test_idempotent_rebuild.py"
      provides: "INGEST-04 自动化证明"
  key_links:
    - from: "ingest_laws.py"
      to: "writer.py"
      via: "CLI 参数驱动 rebuild"
      pattern: "force"
    - from: "writer.py"
      to: "qdrant.py"
      via: "delete/create/upsert"
      pattern: "create_collection|delete_collection|upsert"
    - from: "writer.py"
      to: "audit.py"
      via: "导入结果写审计"
      pattern: "record_ingest_audit"
---

<objective>
交付法规入库的重建写库执行面：在显式 force 保护下删除并重建 `laws_regulations`，完成向量写入和结构化审计输出。

Purpose: 把“可重建、可审计、可回归验证”的闭环跑通，满足 Phase 1 对幂等与入库稳定性的硬约束。  
Output: writer/vectorizer/audit/脚本入口与覆盖 INGEST-02/04 的自动化测试。
</objective>

<execution_context>
@/home/EDY/.cursor/get-shit-done/workflows/execute-plan.md
@/home/EDY/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-fa-gui-shu-ju-ru-ku-di-zuo/01-CONTEXT.md
@.planning/phases/01-fa-gui-shu-ju-ru-ku-di-zuo/01-RESEARCH.md
@.planning/phases/01-fa-gui-shu-ju-ru-ku-di-zuo/01-PATTERNS.md
@.planning/phases/01-fa-gui-shu-ju-ru-ku-di-zuo/01-01-PLAN.md
@bid_tool_agents/backend/app/tools/database/qdrant.py
@bid_tool_agents/backend/app/config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修复 Qdrant 适配并实现 force 全量重建写入（实现 D-07/D-09/D-10/D-11）</name>
  <read_first>
    - bid_tool_agents/backend/app/tools/database/qdrant.py
    - bid_tool_agents/backend/app/config.py
    - .planning/phases/01-fa-gui-shu-ju-ru-ku-di-zuo/01-CONTEXT.md
  </read_first>
  <files>
    bid_tool_agents/backend/app/tools/database/qdrant.py
    bid_tool_agents/backend/app/domain/knowledge_ingest/writer.py
    bid_tool_agents/backend/tests/knowledge_ingest/test_writer.py
  </files>
  <action>
    在 `qdrant.py` 消除本地类与第三方 `QdrantClient` 同名冲突（例如第三方导入别名 + 本地封装重命名）；在 `writer.py` 实现 `rebuild_laws_collection(records, force, vector_size=1536)`：当 `force=False` 直接抛错拒绝删除（D-10），当 `force=True` 执行 `delete_collection -> create_collection(size=1536) -> batch upsert`（D-07/D-09）；集合名固定使用 `laws_regulations` 且仅支持全量重建，不接受 chapter/source 局部过滤参数（D-11）；同时补充 `test_writer.py` 覆盖 force 门禁、调用顺序、批次写入行为。
  </action>
  <verify>
    <automated>cd bid_tool_agents/backend && pytest tests/knowledge_ingest/test_writer.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `force=False` 调用重建函数时返回失败并包含 “force=true required” 语义错误。
    - `force=True` 时，测试可断言先删除后创建再 upsert，且目标集合恒为 `laws_regulations`。
    - `create_collection` 调用参数中的向量维度为 1536。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现向量化与结构化审计记录（实现 D-12/D-16）</name>
  <read_first>
    - bid_tool_agents/backend/app/tools/llm/api/qwen.py
    - bid_tool_agents/backend/app/domain/knowledge_ingest/models.py
    - .planning/phases/01-fa-gui-shu-ju-ru-ku-di-zuo/01-CONTEXT.md
  </read_first>
  <files>
    bid_tool_agents/backend/app/domain/knowledge_ingest/vectorizer.py
    bid_tool_agents/backend/app/domain/knowledge_ingest/audit.py
    bid_tool_agents/backend/app/domain/knowledge_ingest/writer.py
  </files>
  <action>
    在 `vectorizer.py` 提供 `embed_chunks(chunks: list[str]) -> list[list[float]]` 接口，返回长度与输入一致且每个向量长度必须校验为 1536；在 `audit.py` 提供 `record_ingest_audit(result: IngestResult, output_path: str)`，写入结构化 JSON（至少包含开始/结束时间、输入文件列表、总条数、成功数、失败明细、version）；在 `writer.py` 中将导入摘要与错误明细统一汇总到 `IngestResult` 并调用审计记录函数（D-16），失败时也必须写审计条目（D-12）。
  </action>
  <verify>
    <automated>cd bid_tool_agents/backend && pytest tests/knowledge_ingest/test_writer.py -k "audit or embed" -x</automated>
  </verify>
  <acceptance_criteria>
    - 任意向量长度不为 1536 时，向量化流程抛错并阻断写库。
    - 每次执行后都生成一条审计记录，包含 `version`、输入文件、计数统计、错误列表。
    - 导入失败场景下审计记录中的 `errors` 非空，且包含可定位信息。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 提供脚本入口并验证幂等重建（实现 D-13 + INGEST-04）</name>
  <read_first>
    - bid_tool_agents/backend/run_agent_test.py
    - bid_tool_agents/backend/app/domain/knowledge_ingest/loader.py
    - bid_tool_agents/backend/app/domain/knowledge_ingest/writer.py
  </read_first>
  <files>
    bid_tool_agents/backend/app/scripts/ingest_laws.py
    bid_tool_agents/backend/tests/knowledge_ingest/test_idempotent_rebuild.py
  </files>
  <action>
    新增 CLI `ingest_laws.py`，参数至少包含 `--input-dir`、`--version`、`--force`、`--audit-path`；默认不传 `--force` 时拒绝执行重建并返回非 0 退出码；串联 `load_jsonl_records -> embed_chunks -> rebuild_laws_collection -> record_ingest_audit`；输出结构化终端摘要（总数/成功/失败/耗时/版本）；编写 `test_idempotent_rebuild.py`：同一数据集使用 `force=true` 连续执行两次，断言两次成功条数一致、关键 metadata（chunk_id/source_file/version）集合一致，不出现额外重复点。
  </action>
  <verify>
    <automated>cd bid_tool_agents/backend && pytest tests/knowledge_ingest/test_idempotent_rebuild.py -x</automated>
  </verify>
  <acceptance_criteria>
    - 脚本不加 `--force` 时退出码为非 0，并打印拒绝重建提示。
    - 脚本加 `--force` 时可完成全流程并打印结构化摘要与错误明细（无错时 errors 为空列表）。
    - 幂等测试可证明两次重建后记录主集合状态一致，不产生不可控重复。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> rebuild command | 破坏性操作参数从命令行进入执行链路 |
| embedding output -> qdrant writer | 外部/模型输出向量进入持久化系统 |
| ingest result -> audit storage | 执行结果写入审计介质 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | D | `ingest_laws.py` 重建入口 | mitigate | 未显式 `--force` 时拒绝删除与重建，直接退出非 0 |
| T-01-05 | T | `writer.py` payload 写入 | mitigate | 仅写入白名单元数据字段，阻断未定义字段污染 |
| T-01-06 | R | `audit.py` 审计追踪 | mitigate | 每次执行强制写入结构化审计，记录版本/输入/计数/错误 |
| T-01-07 | I | `vectorizer.py` 向量维度 | mitigate | 写库前校验每个向量维度=1536，不符即失败 |
</threat_model>

<verification>
- `cd bid_tool_agents/backend && pytest tests/knowledge_ingest/test_writer.py -x`
- `cd bid_tool_agents/backend && pytest tests/knowledge_ingest/test_idempotent_rebuild.py -x`
</verification>

<success_criteria>
- 法条内容被向量化并写入 `laws_regulations`（INGEST-02）。
- 重建必须 `force=true`，重复执行不产生不可控重复（INGEST-04）。
- 每条记录元数据与版本追溯完整，且导入输出摘要与错误明细（INGEST-03）。
</success_criteria>

<output>
After completion, create `.planning/phases/01-fa-gui-shu-ju-ru-ku-di-zuo/01-02-SUMMARY.md`
</output>
