---
phase: 03-ji-xian-ping-ce-yu-yan-shou
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01-PLAN.md
files_modified:
  - bid_tool_agents/backend/app/scripts/evaluate_knowledge_baseline.py
  - bid_tool_agents/backend/tests/knowledge_eval/test_baseline_acceptance_gate.py
  - bid_tool_agents/backend/tests/api/test_knowledge_query_api.py
autonomous: true
requirements:
  - QUAL-02
  - QUAL-03
must_haves:
  truths:
    - "评测可一键执行并输出可追溯结论（通过/阻塞）。"
    - "当 Recall/MRR/Latency 任一不达阈值时，结果必须阻塞。"
    - "评测过程可识别并报告 API 错误语义异常。"
  artifacts:
    - path: "bid_tool_agents/backend/app/scripts/evaluate_knowledge_baseline.py"
      provides: "离线评测执行入口与阈值闸门"
    - path: "bid_tool_agents/backend/tests/knowledge_eval/test_baseline_acceptance_gate.py"
      provides: "阈值阻塞与报告产出自动化回归"
    - path: "bid_tool_agents/backend/tests/api/test_knowledge_query_api.py"
      provides: "QUAL-03 错误语义回归增强"
  key_links:
    - from: "evaluate_knowledge_baseline.py"
      to: "baseline_eval.py"
      via: "加载样例并聚合指标"
      pattern: "evaluate_cases|summarize_metrics"
    - from: "evaluate_knowledge_baseline.py"
      to: "acceptance thresholds"
      via: "Recall/MRR/Latency gate"
      pattern: "0.80|0.60|1500"
    - from: "test_knowledge_query_api.py"
      to: "knowledge.py error contract"
      via: "异常场景错误字段断言"
      pattern: "error_code|details|request_id"
---

<objective>
将 Phase 3 基线评测闭环化为“可执行 + 可阻塞 + 可修复入口”的验收闸门，并补齐 QUAL-03 异常路径证明。

Purpose: 让评测结果成为发布前硬门槛，而非仅供观察的统计报表。  
Output: 评测脚本入口、阈值阻塞逻辑、验收回归测试与错误语义补强测试。
</objective>

<execution_context>
@/home/EDY/.cursor/get-shit-done/workflows/execute-plan.md
@/home/EDY/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-ji-xian-ping-ce-yu-yan-shou/03-CONTEXT.md
@.planning/phases/03-ji-xian-ping-ce-yu-yan-shou/03-01-PLAN.md
@bid_tool_agents/backend/app/api/v1/knowledge.py
@bid_tool_agents/backend/tests/api/test_knowledge_query_api.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现基线评测执行脚本与阻塞闸门（实现 D-09/D-10/D-11/D-12）</name>
  <files>
    bid_tool_agents/backend/app/scripts/evaluate_knowledge_baseline.py
    bid_tool_agents/backend/tests/knowledge_eval/test_baseline_acceptance_gate.py
  </files>
  <behavior>
    - Test 1: Recall@5 < 0.80 时返回 blocked。
    - Test 2: MRR < 0.60 时返回 blocked。
    - Test 3: latency p95 > 1500ms 时返回 blocked。
  </behavior>
  <action>
    新增脚本 `evaluate_knowledge_baseline.py`，调用 Plan 01 的评测模块执行 30 条样例评测，输出结构化结果（json 或 markdown 二选一，但需含样例文件版本、指标明细、阈值对比）；阈值固定为 Recall>=0.80（D-09）、MRR>=0.60（D-10）、p95<=1500ms（D-11）。任一不达标即标记 `blocked=true` 并以非 0 退出码结束，同时生成 `remediation` 字段作为修复入口（D-12，阻塞而非 warning）。
  </action>
  <verify>
    <automated>cd bid_tool_agents/backend && pytest tests/knowledge_eval/test_baseline_acceptance_gate.py -k "recall_gate or mrr_gate or latency_gate or blocked_exit_code" -x</automated>
  </verify>
  <acceptance_criteria>
    - 脚本输出包含 `recall_at_5`、`mrr`、`latency_ms.p95` 与阈值比较结果。
    - 任一阈值不达标时，进程退出码非 0 且结论为 blocked。
    - 通过场景与阻塞场景都能产出可追溯报告。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 补强 QUAL-03 错误语义验收与异常数据场景（实现 QUAL-03）</name>
  <files>
    bid_tool_agents/backend/tests/api/test_knowledge_query_api.py
    bid_tool_agents/backend/tests/knowledge_eval/test_baseline_acceptance_gate.py
  </files>
  <behavior>
    - Test 1: 查询依赖异常保持 `error_code/message/details/request_id` 结构。
    - Test 2: 评测过程中遇到 503 或异常响应时，报告中记录 failure_reason 与 request_id。
    - Test 3: 数据异常（样例缺字段）触发 fail-fast 并返回可诊断错误。
  </behavior>
  <action>
    在 `test_knowledge_query_api.py` 增加 QUAL-03 强化断言：验证检索失败或数据异常时 API 错误体字段完整且语义明确；在 `test_baseline_acceptance_gate.py` 增加评测侧异常测试，确保 runner 能将 API 异常聚合进最终报告并触发 blocked 结论（不允许吞错继续通过）。该任务遵循保守决策：若遇信息缺口，默认按“阻塞优先、错误显式”处理，不中断规划。
  </action>
  <verify>
    <automated>cd bid_tool_agents/backend && pytest tests/api/test_knowledge_query_api.py tests/knowledge_eval/test_baseline_acceptance_gate.py -k "error_contract or failure_reason or malformed_case" -x</automated>
  </verify>
  <acceptance_criteria>
    - API 异常场景始终返回统一错误契约字段，满足 QUAL-03。
    - 评测报告可定位失败请求与失败原因，支持后续修复。
    - 异常数据不会被静默忽略，评测结论保持 blocked。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| baseline runner -> API dependency | 离线评测依赖在线检索接口，存在可用性与错误传播风险 |
| threshold gate -> release decision | 闸门结论直接影响是否放行，误判会导致质量风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | R | `evaluate_knowledge_baseline.py` 结论输出 | mitigate | 报告写入阈值、原始指标与 blocked 判定，确保结论可追溯 |
| T-03-05 | I | API error payload in reports | mitigate | 报告仅保留 error_code/message/request_id/retryable，避免暴露内部堆栈 |
| T-03-06 | E | acceptance gate bypass | mitigate | 任一阈值或错误契约失败即返回非 0 退出码，杜绝“警告即通过” |
</threat_model>

<verification>
- `cd bid_tool_agents/backend && pytest tests/knowledge_eval/test_baseline_acceptance_gate.py -x`
- `cd bid_tool_agents/backend && pytest tests/api/test_knowledge_query_api.py -k "error_contract or dependency_failure_503" -x`
</verification>

<success_criteria>
- Phase 3 基线评测可执行并输出阈值对比结果（QUAL-02）。
- 阈值门槛严格执行：Recall>=0.80、MRR>=0.60、p95<=1500ms，未达标即阻塞（D-09~D-12）。
- 检索失败或异常数据可被明确识别并输出结构化错误信息（QUAL-03）。
</success_criteria>

<output>
After completion, create `.planning/phases/03-ji-xian-ping-ce-yu-yan-shou/03-02-SUMMARY.md`
</output>
