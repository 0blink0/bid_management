---
phase: 03-ji-xian-ping-ce-yu-yan-shou
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bid_tool_agents/backend/tests/fixtures/knowledge_eval_cases.jsonl
  - bid_tool_agents/backend/app/domain/knowledge_eval/baseline_eval.py
  - bid_tool_agents/backend/tests/knowledge_eval/test_baseline_eval_metrics.py
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
must_haves:
  truths:
    - "评测样例集可稳定复用，且每条样例都可追溯到唯一 golden_chunk_id。"
    - "离线评测可计算 Recall@5、MRR 与延时统计（avg/p50/p95）。"
    - "评测结果对同一输入具备可复现性，能够作为后续验收基线。"
  artifacts:
    - path: "bid_tool_agents/backend/tests/fixtures/knowledge_eval_cases.jsonl"
      provides: "30 条基线评测样例（query + golden_chunk_id）"
    - path: "bid_tool_agents/backend/app/domain/knowledge_eval/baseline_eval.py"
      provides: "样例加载、指标计算与延时统计逻辑"
    - path: "bid_tool_agents/backend/tests/knowledge_eval/test_baseline_eval_metrics.py"
      provides: "QUAL-01/02 自动化回归"
  key_links:
    - from: "baseline_eval.py"
      to: "knowledge.py /query contract"
      via: "按 query 调用检索并读取 top5 chunk_id"
      pattern: "top_k|chunk_id|recall_at_5"
    - from: "baseline_eval.py"
      to: "knowledge_eval_cases.jsonl"
      via: "逐行加载 query + golden_chunk_id"
      pattern: "golden_chunk_id"
    - from: "test_baseline_eval_metrics.py"
      to: "baseline_eval.py"
      via: "指标口径与样例约束断言"
      pattern: "Recall@5|MRR|p95"
---

<objective>
交付 Phase 3 的评测基线核心：固定样例集与指标计算器，为验收阻塞提供可复现数据基础。

Purpose: 将“评测是否通过”从主观判断转为可自动化执行的量化结论。  
Output: 30 条样例数据、评测指标计算模块与自动化回归测试。
</objective>

<execution_context>
@/home/EDY/.cursor/get-shit-done/workflows/execute-plan.md
@/home/EDY/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-ji-xian-ping-ce-yu-yan-shou/03-CONTEXT.md
@.planning/phases/02-fa-gui-zhi-shi-ku-api/02-01-SUMMARY.md
@bid_tool_agents/backend/app/api/v1/knowledge.py
@bid_tool_agents/backend/tests/api/test_knowledge_query_api.py

<interfaces>
From `bid_tool_agents/backend/app/api/v1/knowledge.py`:
- `POST /api/v1/knowledge/query` returns `items/total/took_ms/trace`
- each hit includes `chunk_id` and `score`
- dependency failure returns 503 with `error_code/message/details/request_id`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 构建 30 条固定评测样例集（实现 D-01/D-02/D-03/D-04）</name>
  <files>
    bid_tool_agents/backend/tests/fixtures/knowledge_eval_cases.jsonl
  </files>
  <behavior>
    - Test 1: 文件总行数固定为 30 条，不多不少。
    - Test 2: 每条样例严格包含 `query` 与 `golden_chunk_id` 两个核心字段。
    - Test 3: 样例全部来源于既有法规 JSONL 映射的 chunk_id，不引入外部数据源。
  </behavior>
  <action>
    在 `tests/fixtures/knowledge_eval_cases.jsonl` 新建 Phase 3 基线样例集，逐条写入 `{"query": "...", "golden_chunk_id": "..."}`；样例数量固定 30（D-02），字段口径固定为 query+golden_chunk_id（D-03），来源限定为现有法规 JSONL 的已入库 chunk（D-01）；文件路径必须固定在 `tests/fixtures`（D-04）。禁止加入多 gold、多知识库字段（deferred）。
  </action>
  <verify>
    <automated>cd bid_tool_agents/backend && pytest tests/knowledge_eval/test_baseline_eval_metrics.py -k "fixture_shape or fixture_count" -x</automated>
  </verify>
  <acceptance_criteria>
    - 样例文件存在且总条数为 30。
    - 每条记录都能解析出非空 `query` 与 `golden_chunk_id`。
    - 无任何记录包含本阶段 deferred 的扩展标注结构。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 Recall@5/MRR/Latency 统计器（实现 D-05/D-06/D-07/D-08）</name>
  <files>
    bid_tool_agents/backend/app/domain/knowledge_eval/baseline_eval.py
    bid_tool_agents/backend/tests/knowledge_eval/test_baseline_eval_metrics.py
  </files>
  <behavior>
    - Test 1: Recall@5 仅在 top5 命中任一 `golden_chunk_id` 时计 1。
    - Test 2: MRR 使用首个相关命中倒数排名，未命中记 0。
    - Test 3: 延时统计同时输出 avg、p50、p95，并保持毫秒单位。
  </behavior>
  <action>
    新建 `baseline_eval.py`，提供 `load_eval_cases`、`evaluate_cases`、`summarize_metrics`：对每个 query 调用现有检索契约，读取 top5 结果并按 D-05 计算 Recall@5，按 D-06 计算 MRR，按 D-07 统计 latency(avg/p50/p95)；默认 `knowledge_type` 固定为 `laws_regulations`（D-08）。补充 `test_baseline_eval_metrics.py`，覆盖指标公式、边界值与输出字段完整性，确保 QUAL-02 可自动回归。
  </action>
  <verify>
    <automated>cd bid_tool_agents/backend && pytest tests/knowledge_eval/test_baseline_eval_metrics.py -k "recall_at_5 or mrr or latency_summary" -x</automated>
  </verify>
  <acceptance_criteria>
    - 评测结果对象包含 `recall_at_5`、`mrr`、`latency_ms.avg`、`latency_ms.p50`、`latency_ms.p95`。
    - 指标口径与 D-05/D-06/D-07 完全一致，测试可证明公式正确。
    - 默认评测范围固定为 `laws_regulations` 且可追踪到 case 文件版本。
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval fixture -> metric engine | 样例文件内容进入评测逻辑，可能包含脏数据 |
| metric engine -> query API | 评测模块依赖 API 返回结构，异常会影响结论正确性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `knowledge_eval_cases.jsonl` | mitigate | 对每条样例执行 schema 校验（必须含 query/golden_chunk_id）并在加载阶段 fail-fast |
| T-03-02 | R | `baseline_eval.py` 指标输出 | mitigate | 固定指标公式与输出字段，所有计算步骤由自动化测试锁定 |
| T-03-03 | D | 评测批量调用 `/query` | mitigate | 逐条记录请求耗时与失败原因，避免单次异常吞掉整批结论 |
</threat_model>

<verification>
- `cd bid_tool_agents/backend && pytest tests/knowledge_eval/test_baseline_eval_metrics.py -x`
</verification>

<success_criteria>
- 已具备可复用的 30 条评测样例与离线评测入口（QUAL-01）。
- 评测输出可稳定提供 Recall@5、MRR、Latency(avg/p50/p95)（QUAL-02）。
- 指标公式和样例约束可通过自动化测试反复验证。
</success_criteria>

<output>
After completion, create `.planning/phases/03-ji-xian-ping-ce-yu-yan-shou/03-01-SUMMARY.md`
</output>
